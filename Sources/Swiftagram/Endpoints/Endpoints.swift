//
//  Endpoint.swift
//  Swiftagram
//
//  Created by Stefano Bertagno on 05/12/22.
//

import Foundation

import Requests

public extension Endpoints {
    /// A module-like `enum` containing all
    /// archives-related endpoints.
    enum Archives { }

    /// A module-like `enum` containing all
    /// explore-related endpoints.
    enum Explore { }

    /// A module-like `enum` containing all
    /// location-aware endpoints.
    enum Map { }

    /// A module-like `enum` containing
    /// timeline, stories tray and other time
    /// sensitive endpoints.
    enum Recent { }

    /// A module-like `enum` containing all
    /// safe-related endpoints.
    enum Safe { }
}
